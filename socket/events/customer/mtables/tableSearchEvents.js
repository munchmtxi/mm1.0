'use strict';

const socketConstants = require('@constants/common/socketConstants');

/** Customer MTables socket event constants */
module.exports = {
  TABLE_SEARCH_RESULT: socketConstants.SOCKET_EVENT_TYPES.TABLE_SEARCH_RESULT,
};